#!/usr/bin/env node

var program = require('commander');

program
  .command('server', 'Manage local dozer server')
  .command('tokens', 'Manage auth tokens for the local dozer server')
  .command('client', 'Issue requests to a dozer server')
  .parse(process.argv);
